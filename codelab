import random
userName = str(input('Введите ваше имя:'))
if len(userName) > 360:
    print ("Вы превысили число допустимых символов!")
else:
    print ("Вы зарегистрированы")
userEmail = str(input("Введите вашу почту:"))
if len(userEmail) > 600:
    print ("Вы превысили число допустимых символов!")
else:
    print ("Вы зарегистрированы")
userId = sum(random() <= 1 > 99999999)
userPnum = sum(random() > 0 < 20)



manageName = str(input('Введите ваше имя:'))
if len(manageName) > 360:
    print ("Вы превысили число допустимых символов!")
else:
    print ("Вы зарегистрированы")
manageEmail = str(input("Введите вашу почту:"))
if len(manageEmail) > 600:
    print ("Вы превысили число допустимых символов!")
else:
    print ("Вы зарегистрированы")
manageId = sum(random() <= 1 > 99999999)
managePnum = sum(random() > 0 < 20)
